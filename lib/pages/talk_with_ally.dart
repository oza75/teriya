import 'package:flutter/cupertino.dart';

class TalkWithAlly extends StatelessWidget {
  const TalkWithAlly({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Talk with ally page"),
    );
  }
}
